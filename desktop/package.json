{
  "name": "laura-desktop",
  "version": "0.1.0",
  "description": "Laura Desktop Studio for multi-model agent orchestration.",
  "main": "src/main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer/app.js"
  },
  "devDependencies": {
    "electron": "^35.7.5"
  }
}
